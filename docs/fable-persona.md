# SYSTEM PERSONA
You are a Staff-Level AI Engineer and Systems Architect. I have a 5-hour block to work with you, and I need you to build a production-ready, highly resilient downstream pipeline.

# CONTEXT & CURRENT STATE
I have a local transcription service called `voicebox`. It runs locally and successfully allocates to my `meta` hardware device. The Whisper model loads perfectly, and the raw Transcription API is stable and functioning. 

However, I am currently losing ideas in the middle of the day. I need to be able to dictate audio (brain-dumps, action items, shower thoughts), have them hit the `voicebox` Whisper API, and immediately get processed into structured intelligence. 

# THE OBJECTIVE
Your task is to build the **Voicebox Intelligence Extraction Pipeline**. This is a downstream processing layer that sits immediately after the raw Whisper transcription. 

When a raw transcript is generated by `voicebox`, this new pipeline must automatically intercept the text, understand the semantic intent, and route the extracted information to the correct systems so nothing is ever lost.

# TECHNICAL REQUIREMENTS
Please architect and write the complete codebase for this pipeline with the following capabilities:

1. **The Ingestion Engine (The Webhook/Listener):**
   - Create a lightweight service (Python FastAPI or Node.js) that acts as the primary receiver for the `voicebox` Whisper API output.

2. **The Intelligence Router (LLM Processing):**
   - Once the raw text is received, pass it through an LLM extraction function. 
   - You can assume I have a local API available (e.g., `freellmapi` running locally). 
   - The LLM step must classify the transcript and extract:
     - **Action Items** (things I need to do).
     - **Core Ideas/Insights** (general thoughts or architectural notes).
     - **Summaries** (a TL;DR of the brain dump).

3. **The Output Sinks (Where the data goes):**
   - **Todoist Sync:** Any extracted "Action Items" must be formatted and prepared for API dispatch to Todoist (using my existing Todoist shadow map structure).
   - **Agentic Wiki Inbox:** Any "Core Ideas" or summaries must be formatted into clean Markdown files (complete with YAML frontmatter, timestamps, and tags) and written directly to my Obsidian vault at `/root/agentic-wiki/00-INBOX/`. 

# INSTRUCTIONS FOR THIS SESSION
We have 5 hours to complete this. Please start by providing:
1. **The Architecture Blueprint:** A quick high-level overview of the folders, scripts, and libraries we will use.
2. **The Ingestion & Router Code:** The exact code for the FastAPI/Node service that receives the transcript and hits the LLM for extraction.
3. **The Markdown/Todoist Generators:** The utility functions that parse the LLM JSON output and create the `.md` files for my vault and the payloads for Todoist.

Assume I will paste the current `voicebox` API routing code in my next prompt so you can see exactly how to wire it up. Let's build this so I never lose a mid-day idea again. Ready?
